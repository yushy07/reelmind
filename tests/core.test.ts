import {test} from 'node:test';
import assert from 'node:assert/strict';
import { mkdtemp, mkdir, writeFile, readFile, rm } from 'node:fs/promises';
import path from 'node:path';
import os from 'node:os';
import {validateUrl,selectCandidates,makeCaptions,planEdit,settingsSchema} from '../electron/core';
import {externalDirectory,removeWorkspace,Store,defaults} from '../electron/storage';
import {analyze} from '../electron/providers';
import type {Transcript,Candidate} from '../shared/types';
const t:Transcript={version:1,duration:180,language:'en',segments:Array.from({length:18},(_,i)=>({start:i*10,end:i*10+9.9,text:i%4===0?'Why did this change everything?':'Because I learned the lesson.','language':'en',words:Array.from({length:20},(_,j)=>({start:i*10+j*.49,end:i*10+j*.49+.4,text:j===0?'Why':'lesson'}))}))};
const candidate=(start=0,score=90):Candidate=>({start,end:start+45,hook:'A surprising story '+start,context:'Context '+start,payoff:'Lesson learned.',reason:'Clear lesson',category:'story',score});
test('video link boundaries',()=>{assert.match(validateUrl('https://youtu.be/example'),/youtu.be/);assert.match(validateUrl('https://example.com/a.mp4'),/mp4/);for(const u of ['file:///x','http://example.com/a.mp4','https://localhost/a.mp4','https://127.0.0.1/a.mp4','https://example.com/private','https://u:p@example.com/a.mp4'])assert.throws(()=>validateUrl(u));});
test('candidate ranking removes overlap, weak moments and invalid durations',()=>{const selected=selectCandidates([candidate(),candidate(1,80),candidate(80,30),{...candidate(),end:90}],t);assert.equal(selected.length,1);assert.ok(selected[0].end-selected[0].start>=30);assert.ok(selected[0].end-selected[0].start<=60);});
test('captions preserve languages, escape ASS controls and highlight words',()=>{const ass=makeCaptions([{start:0,end:1,text:'नमस्ते'},{start:1,end:2,text:'世界'},{start:2,end:3,text:'{\\bad}'}]);assert.ok(ass.includes('नमस्ते'));assert.ok(ass.includes('世界'));assert.ok(!ass.includes('{\\bad}'));assert.ok(ass.includes('\\t('));});
test('captions reset on sentences and Japanese script changes',()=>{const ass=makeCaptions([{start:0,end:1,text:'नमस्ते।'},{start:1.1,end:2,text:'世界'},{start:2.1,end:3,text:'Hello.'}]);const lines=ass.split('\n').filter(s=>s.startsWith('Dialogue:'));assert.equal(lines.length,3);assert.ok(!lines[1].includes('नमस्ते'));assert.ok(!lines[2].includes('世界'));});
test('edit plans preserve source audio, valid timing and safe unknown framing',()=>{const plan=planEdit(candidate(),t,[]);assert.equal(plan.audio.sourceOnly,true);assert.ok(plan.duration>=30&&plan.duration<=60);assert.equal(plan.shots[0].layout,'fit');assert.ok(plan.words.every(w=>w.start>=0&&w.end<=plan.duration));});
test('edit plans follow content beats and enable 60 FPS only for high-motion sources',()=>{const frames=Array.from({length:46},(_,i)=>({time:i,faces:[{x:(i%2)*.25+.2,y:.1,w:.2,h:.3,confidence:.9,track:1,motion:.1}],activeTrack:1,confidence:.9}));const plan=planEdit(candidate(),t,frames,60);assert.equal(plan.version,2);assert.equal(plan.fps,60);assert.ok(plan.shots.every(s=>s.end-s.start<=5.21));assert.ok(plan.shots.some(s=>s.transition==='punch'));assert.ok(plan.shots.some(s=>s.endCenter!==s.center));});
test('settings reject paid OpenRouter models',()=>{assert.ok(settingsSchema.safeParse(defaults).success);assert.ok(!settingsSchema.safeParse({...defaults,openrouterModel:'openai/gpt-4o'}).success);});
test('provider quota failure falls through without retrying paid paths',async()=>{const calls:string[]=[];const mock=async(url:any)=>{calls.push(String(url));return String(url).includes('googleapis')?new Response('',{status:429}):Response.json({choices:[{message:{content:JSON.stringify({candidates:[candidate()]})}}]});};const result=await analyze(t,{...defaults,geminiFreeConfirmed:true},async()=> 'test-key',new AbortController().signal,()=>{},mock as typeof fetch);assert.equal(result.length,1);assert.equal(calls.length,2);});
test('malformed responses retry once then local fallback completes',async()=>{let calls=0;const result=await analyze(t,{...defaults,geminiFreeConfirmed:true},async()=> 'test-key',new AbortController().signal,()=>{},(async()=>{calls++;return Response.json({});}) as typeof fetch);assert.equal(calls,4);assert.ok(Array.isArray(result));});
test('network failures switch providers immediately without retrying',async()=>{let calls=0;const result=await analyze(t,{...defaults,geminiFreeConfirmed:true},async()=> 'test-key',new AbortController().signal,()=>{},(async()=>{calls++;throw new TypeError('Network unavailable');}) as typeof fetch);assert.equal(calls,2);assert.ok(Array.isArray(result));});
test('multiple cloud candidates receive a whole-video ranking pass',async()=>{
 let calls=0;const distinct={...candidate(80,88),hook:'Unexpected career confession',context:'A completely different topic',payoff:'The audience reacted.'};const first={candidates:[candidate(0,91),distinct]},final={candidates:[distinct]};
 const mock=async(_url:unknown,init?:RequestInit)=>{calls++;let answer=first;if(calls>1){const body=JSON.parse(String(init?.body));const prompt=body.contents?.[0]?.parts?.[0]?.text||body.messages?.[0]?.content;const globallyRanked=JSON.parse(prompt.split('Candidates:\n')[1]);answer={candidates:[globallyRanked[1]]};}return Response.json({candidates:[{content:{parts:[{text:JSON.stringify(answer)}]}}]});};
 const result=await analyze(t,{...defaults,geminiFreeConfirmed:true},async()=> 'test-key',new AbortController().signal,()=>{},mock as typeof fetch);
 assert.equal(calls,2);assert.equal(result.length,1);assert.ok(Math.abs(result[0].start-80)<.1);
});
test('storage restricts export and deletion; originals survive',async()=>{const root=await mkdtemp(path.join(os.tmpdir(),'reelmind-test-'));try{const work=path.join(root,'work');const job=path.join(work,'job');const external=path.join(root,'external');await mkdir(job,{recursive:true});await mkdir(external);await writeFile(path.join(external,'source.mp4'),'original');await assert.rejects(()=>externalDirectory(job,[work]));assert.equal(await externalDirectory(external,[work]),external);await assert.rejects(()=>removeWorkspace(work,external));await assert.rejects(()=>removeWorkspace(work,work));await removeWorkspace(work,job);assert.equal(await readFile(path.join(external,'source.mp4'),'utf8'),'original');}finally{await rm(root,{recursive:true,force:true});}});
test('SQLite retains settings across reconnect',async()=>{const dir=await mkdtemp(path.join(os.tmpdir(),'reelmind-db-'));try{const file=path.join(dir,'db.sqlite');const first=new Store(file);first.setSettings({...defaults,quality:'high'});first.db.close();const second=new Store(file);assert.equal(second.settings().quality,'high');second.db.close();}finally{await rm(dir,{recursive:true,force:true});}});
